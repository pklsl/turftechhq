version: 0.0
os: linux
files:
  - source: dist/
    destination: /var/www/turftechhq
hooks:
  BeforeInstall:
    - location: scripts/before_install.sh
      timeout: 30
      runas: root
  AfterInstall:
    - location: scripts/after_install.sh
      timeout: 30
      runas: root
