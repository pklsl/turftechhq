---
interface Props {
  siteId: string;
  placementType: 'inline' | 'cta' | 'exit-intent';
  leadMagnetLabel?: string;
}

const SITE_TAG_IDS: Record<string, number> = {
  thermaltechpro: 18909240,
  flowcontrolhq: 18909241,
  shinglescience: 18909242,
  amperagehq: 18909243,
  thepestpro: 18909244,
  theweldersguide: 18909245,
  ironworksinsider: 18909246,
  turftechhq: 18909247,
  thecarpentersguide: 18909248,
};

const LEAD_MAGNET_URLS: Record<string, string> = {
  thermaltechpro: 'https://conduit-media-lead-magnets-660118042145.s3.us-east-1.amazonaws.com/hvac-maintenance-checklist.pdf',
  flowcontrolhq: 'https://conduit-media-lead-magnets-660118042145.s3.us-east-1.amazonaws.com/plumbing-inspection-checklist.pdf',
  shinglescience: 'https://conduit-media-lead-magnets-660118042145.s3.us-east-1.amazonaws.com/roof-insurance-checklist.pdf',
  amperagehq: 'https://conduit-media-lead-magnets-660118042145.s3.us-east-1.amazonaws.com/electrical-safety-checklist.pdf',
  thepestpro: 'https://conduit-media-lead-magnets-660118042145.s3.us-east-1.amazonaws.com/termite-prevention-guide.pdf',
  theweldersguide: 'https://conduit-media-lead-magnets-660118042145.s3.us-east-1.amazonaws.com/welding-safety-checklist.pdf',
  ironworksinsider: 'https://conduit-media-lead-magnets-660118042145.s3.us-east-1.amazonaws.com/heavy-equipment-buyers-checklist.pdf',
  turftechhq: 'https://conduit-media-lead-magnets-660118042145.s3.us-east-1.amazonaws.com/lawn-care-seasonal-calendar.pdf',
  thecarpentersguide: 'https://conduit-media-lead-magnets-660118042145.s3.us-east-1.amazonaws.com/woodworking-tool-checklist.pdf',
};

const {
  siteId,
  placementType,
  leadMagnetLabel = 'Free Resource',
} = Astro.props;

const ckApiKey = import.meta.env.PUBLIC_CK_API_KEY || 'kit_c9479f8706aba6b9e2e8a9d2ef9407d0';
const ckTagId = SITE_TAG_IDS[siteId] || 0;
const leadMagnetUrl = LEAD_MAGNET_URLS[siteId] || '';
const uid = `ec-${placementType}-${Math.random().toString(36).slice(2, 7)}`;
---

{placementType === 'inline' && (
  <div class="email-capture-inline my-6 p-4 bg-primary/5 border border-primary/20 rounded-lg" id={uid} data-placement="inline" data-site={siteId}>
    <p class="text-sm font-semibold text-text mb-2">Get our free {leadMagnetLabel} — delivered to your inbox.</p>
    <form class="ck-form flex gap-2">
      <input
        type="email"
        name="email"
        required
        placeholder="Enter your email"
        class="flex-1 px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary focus:border-transparent outline-none text-sm"
      />
      <button
        type="submit"
        class="bg-primary hover:bg-primary-dark text-white font-semibold px-4 py-2 rounded-lg transition-colors duration-200 text-sm whitespace-nowrap"
      >
        Get It Free
      </button>
    </form>
    <p class="text-xs text-text-muted mt-2">No spam. Unsubscribe anytime.</p>
  </div>
)}

{placementType === 'cta' && (
  <div class="email-capture-cta my-12 bg-gradient-to-br from-primary to-primary-dark text-white rounded-xl p-8 md:p-12" id={uid} data-placement="cta" data-site={siteId}>
    <div class="max-w-xl mx-auto text-center">
      <h2 class="text-2xl md:text-3xl font-bold mb-3 font-heading">Get Your Free {leadMagnetLabel}</h2>
      <p class="text-blue-100 mb-6">Join thousands of readers. Expert tips and guides delivered to your inbox — no spam, ever.</p>
      <form class="ck-form space-y-3">
        <input
          type="text"
          name="first_name"
          placeholder="First name (optional)"
          class="w-full px-4 py-3 border border-blue-400 rounded-lg focus:ring-2 focus:ring-accent focus:border-transparent outline-none text-gray-900 placeholder-gray-400"
        />
        <input
          type="email"
          name="email"
          required
          placeholder="Enter your email address"
          class="w-full px-4 py-3 border border-blue-400 rounded-lg focus:ring-2 focus:ring-accent focus:border-transparent outline-none text-gray-900 placeholder-gray-500"
        />
        <button
          type="submit"
          class="w-full bg-accent hover:bg-accent-light text-white font-semibold px-6 py-3 rounded-lg transition-colors duration-200"
        >
          Get the Free {leadMagnetLabel}
        </button>
      </form>
      <p class="text-xs text-blue-200 mt-4">
        No spam. Unsubscribe anytime. View our <a href="/privacy-policy" class="text-accent hover:text-accent-light underline">Privacy Policy</a>.
      </p>
    </div>
  </div>
)}

{placementType === 'exit-intent' && (
  <div class="email-capture-exit hidden fixed inset-0 z-50 flex items-center justify-center p-4 bg-black/50" id={uid} data-placement="exit-intent" data-site={siteId}>
    <div class="bg-white rounded-2xl shadow-2xl max-w-md w-full p-8 relative">
      <button class="ck-exit-close absolute top-4 right-4 text-gray-400 hover:text-gray-600 text-2xl leading-none">&times;</button>
      <h2 class="text-2xl font-bold mb-2 font-heading text-text">Wait — Before You Go!</h2>
      <p class="text-text-muted mb-6">Grab your free {leadMagnetLabel} before you leave. Join thousands of readers who get our best tips.</p>
      <form class="ck-form space-y-3">
        <input
          type="text"
          name="first_name"
          placeholder="First name (optional)"
          class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary focus:border-transparent outline-none text-sm"
        />
        <input
          type="email"
          name="email"
          required
          placeholder="Enter your email address"
          class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary focus:border-transparent outline-none text-sm"
        />
        <button
          type="submit"
          class="w-full bg-primary hover:bg-primary-dark text-white font-semibold px-6 py-3 rounded-lg transition-colors duration-200"
        >
          Get It Free
        </button>
      </form>
      <p class="text-xs text-text-muted mt-3 text-center">No spam. Unsubscribe anytime.</p>
    </div>
  </div>
)}

<script define:vars={{ uid, ckApiKey, ckTagId, placementType, leadMagnetUrl, leadMagnetLabel }}>
  (function () {
    var container = document.getElementById(uid);
    if (!container) return;

    if (placementType === 'exit-intent') {
      var shown = sessionStorage.getItem('ck-exit-shown-' + uid);
      if (!shown) {
        document.addEventListener('mouseleave', function onLeave(e) {
          if (e.clientY <= 0) {
            container.classList.remove('hidden');
            container.classList.add('flex');
            sessionStorage.setItem('ck-exit-shown-' + uid, '1');
            document.removeEventListener('mouseleave', onLeave);
          }
        });
      }
      var closeBtn = container.querySelector('.ck-exit-close');
      if (closeBtn) {
        closeBtn.addEventListener('click', function () {
          container.classList.add('hidden');
          container.classList.remove('flex');
        });
      }
      container.addEventListener('click', function (e) {
        if (e.target === container) {
          container.classList.add('hidden');
          container.classList.remove('flex');
        }
      });
    }

    var form = container.querySelector('.ck-form');
    if (!form) return;

    form.addEventListener('submit', function (e) {
      e.preventDefault();
      var emailInput = form.querySelector('input[name="email"]');
      var firstNameInput = form.querySelector('input[name="first_name"]');
      var btn = form.querySelector('button[type="submit"]');
      var email = emailInput ? emailInput.value.trim() : '';
      var firstName = firstNameInput ? firstNameInput.value.trim() : '';

      if (!email) return;

      if (!ckApiKey || !ckTagId) {
        console.warn('[EmailCapture] ConvertKit not configured.');
        form.innerHTML = '<p class="text-sm text-center text-text-muted py-4">Email capture is not yet configured. Please check back soon!</p>';
        return;
      }

      btn.disabled = true;
      var origText = btn.textContent;
      btn.textContent = 'Subscribing\u2026';

      var subscriberPayload = { email_address: email };
      if (firstName) subscriberPayload.first_name = firstName;

      fetch('https://api.kit.com/v4/subscribers', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json', 'X-Kit-Api-Key': ckApiKey },
        body: JSON.stringify(subscriberPayload),
      })
        .then(function () {
          return fetch('https://api.kit.com/v4/tags/' + ckTagId + '/subscribers', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json', 'X-Kit-Api-Key': ckApiKey },
            body: JSON.stringify({ email_address: email, first_name: firstName || undefined }),
          });
        })
        .then(function (res) { return res.json(); })
        .then(function (data) {
          if (data.subscriber) {
            var downloadFilename = leadMagnetUrl ? leadMagnetUrl.split('/').pop() : '';
            var downloadHtml = leadMagnetUrl
              ? '<a href="#" class="lead-magnet-download-btn" style="display:inline-block;margin-top:12px;padding:10px 24px;background:var(--color-accent, #f59e0b);color:#fff;border-radius:8px;text-decoration:none;font-weight:600;font-size:14px;">Download Your ' + leadMagnetLabel + '</a>'
              : '';
            form.innerHTML =
              '<div class="text-center py-4">' +
              '<p class="text-lg font-semibold mb-1">You\'re subscribed!</p>' +
              '<p class="text-sm text-text-muted mb-3">Your ' + leadMagnetLabel + ' is ready.</p>' +
              downloadHtml +
              '</div>';
            var dlBtn = form.querySelector('.lead-magnet-download-btn');
            if (dlBtn) {
              dlBtn.addEventListener('click', function(ev) {
                ev.preventDefault();
                fetch(leadMagnetUrl)
                  .then(function(r) { return r.blob(); })
                  .then(function(blob) {
                    var url = URL.createObjectURL(blob);
                    var a = document.createElement('a');
                    a.href = url;
                    a.download = downloadFilename;
                    a.style.display = 'none';
                    document.body.appendChild(a);
                    a.click();
                    setTimeout(function() { document.body.removeChild(a); URL.revokeObjectURL(url); }, 200);
                  })
                  .catch(function() { window.open(leadMagnetUrl, '_blank'); });
              });
            }
          } else {
            btn.disabled = false;
            btn.textContent = origText;
            var errMsg = (data.errors && data.errors[0]) || 'Something went wrong. Please try again.';
            alert(errMsg);
          }
        })
        .catch(function () {
          btn.disabled = false;
          btn.textContent = origText;
          alert('Sorry, there was a network error. Please try again.');
        });
    });
  })();
</script>
